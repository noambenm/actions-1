name: My First Workflow

on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: exec shell
        run: |
          chmod +x ./asci.sh
          ./asci.sh
